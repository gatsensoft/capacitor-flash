import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitor.flash',
  appName: 'capacitor-flash',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
